<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ShortFormLabs | Atlanta Lead Automation</title>
  <meta name="description" content="ShortFormLabs helps Atlanta businesses capture leads, follow up automatically, and book more appointments."/>
  <style>
    :root{
      --bg:#0b0c10;
      --card:#12141b;
      --text:#e9ecf1;
      --muted:#a6adbb;
      --line:#232838;
      --accent:#7c5cff;
      --accent2:#2ee59d;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Apple Color Emoji","Segoe UI Emoji";
      background: radial-gradient(1000px 600px at 20% 10%, rgba(124,92,255,.18), transparent 60%),
                  radial-gradient(1000px 600px at 80% 20%, rgba(46,229,157,.12), transparent 55%),
                  var(--bg);
      color:var(--text);
      line-height:1.5;
    }
    a{color:inherit; text-decoration:none}
    .wrap{max-width:1050px; margin:0 auto; padding:28px 18px 80px;}
    .nav{
      display:flex; align-items:center; justify-content:space-between;
      border:1px solid var(--line);
      background: rgba(18,20,27,.7);
      backdrop-filter: blur(10px);
      border-radius:16px;
      padding:12px 14px;
      position:sticky; top:14px; z-index:10;
    }
    .brand{
      display:flex; gap:10px; align-items:center;
      font-weight:700; letter-spacing:.3px;
    }
    .logo{
      width:34px; height:34px; border-radius:12px;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      box-shadow: 0 10px 40px rgba(124,92,255,.25);
    }
    .navlinks{display:flex; gap:16px; align-items:center; flex-wrap:wrap;}
    .pill{
      padding:9px 12px;
      border:1px solid var(--line);
      border-radius:999px;
      color:var(--muted);
      font-size:14px;
    }
    .btn{
      display:inline-flex; align-items:center; justify-content:center;
      gap:8px;
      padding:11px 14px;
      border-radius:12px;
      border:1px solid var(--line);
      background: rgba(255,255,255,.02);
      font-weight:650;
      font-size:14px;
      transition: transform .12s ease, border-color .12s ease;
    }
    .btn:hover{transform: translateY(-1px); border-color: rgba(124,92,255,.55)}
    .btn.primary{
      border: none;
      background: linear-gradient(135deg, var(--accent), #5b8cff);
      box-shadow: 0 16px 45px rgba(124,92,255,.22);
    }
    .hero{padding:56px 6px 26px;}
    .kicker{
      display:inline-flex; gap:10px; align-items:center;
      padding:8px 12px; border-radius:999px;
      border:1px solid var(--line);
      background: rgba(18,20,27,.55);
      color: var(--muted);
      font-size:13px;
    }
    .dot{width:8px; height:8px; border-radius:999px; background: var(--accent2);}
    h1{margin:16px 0 10px; font-size: clamp(34px, 4.2vw, 56px); line-height:1.05;}
    .sub{max-width:740px; color:var(--muted); font-size: clamp(15px, 1.4vw, 18px);}
    .cta{display:flex; gap:12px; flex-wrap:wrap; margin-top:18px;}
    .grid{
      display:grid;
      grid-template-columns: repeat(12, 1fr);
      gap:14px;
      margin-top:24px;
    }
    .card{
      grid-column: span 12;
      border:1px solid var(--line);
      background: rgba(18,20,27,.65);
      border-radius:18px;
      padding:18px;
    }
    @media(min-width:820px){
      .card.third{grid-column: span 4;}
      .card.half{grid-column: span 6;}
    }
    .card h3{margin:0 0 8px; font-size:18px;}
    .card p{margin:0; color:var(--muted)}
    .section{margin-top:46px;}
    .section h2{margin:0 0 10px; font-size:26px;}
    .section .desc{margin:0 0 18px; color:var(--muted); max-width:780px;}
    .steps{display:grid; gap:12px;}
    @media(min-width:820px){ .steps{grid-template-columns: repeat(3, 1fr);} }
    .step{
      border:1px solid var(--line);
      background: rgba(18,20,27,.55);
      border-radius:18px;
      padding:16px;
      min-height:120px;
    }
    .num{
      width:28px; height:28px; border-radius:10px;
      display:flex; align-items:center; justify-content:center;
      background: rgba(124,92,255,.15);
      border:1px solid rgba(124,92,255,.35);
      font-weight:700;
      margin-bottom:10px;
    }
    .list{display:grid; gap:10px;}
    .item{
      display:flex; gap:10px; align-items:flex-start;
      padding:12px 12px;
      border:1px solid var(--line);
      background: rgba(18,20,27,.55);
      border-radius:14px;
    }
    .check{
      width:18px; height:18px; border-radius:6px;
      background: rgba(46,229,157,.16);
      border:1px solid rgba(46,229,157,.45);
      margin-top:2px;
      flex:0 0 auto;
    }
    form{
      display:grid; gap:12px;
    }
    .row{display:grid; gap:12px;}
    @media(min-width:820px){ .row{grid-template-columns: 1fr 1fr;} }
    input, textarea{
      width:100%;
      padding:12px 12px;
      border-radius:12px;
      border:1px solid var(--line);
      background: rgba(9,10,14,.55);
      color:var(--text);
      outline:none;
    }
    textarea{min-height:110px; resize:vertical;}
    .footer{
      margin-top:44px;
      padding-top:18px;
      border-top:1px solid var(--line);
      color:var(--muted);
      font-size:13px;
      display:flex; gap:10px; justify-content:space-between; flex-wrap:wrap;
    }
    .small{font-size:13px; color:var(--muted)}
    .tag{
      display:inline-flex; align-items:center; gap:8px;
      padding:8px 10px; border-radius:999px;
      border:1px solid var(--line);
      background: rgba(18,20,27,.55);
      color:var(--muted);
      font-size:13px;
      margin-right:8px;
      margin-top:8px;
    }
  </style>
</head>

<body>
  <div class="wrap">

    <header class="nav">
      <div class="brand">
        <div class="logo" aria-hidden="true"></div>
        <div>ShortFormLabs</div>
      </div>
      <nav class="navlinks">
        <a class="pill" href="#how">How it works</a>
        <a class="pill" href="#offer">What you get</a>
        <a class="pill" href="#contact">Contact</a>
        <a class="btn primary" href="#contact">Free Setup Plan</a>
      </nav>
    </header>

    <section class="hero">
      <div class="kicker"><span class="dot"></span> Atlanta • Lead capture + follow-up + booking</div>
      <h1>Stop losing leads after hours.</h1>
      <p class="sub">
        ShortFormLabs sets up simple systems that instantly respond to new inquiries,
        follow up automatically, and push leads to your booking calendar — so you get more appointments without extra work.
      </p>

      <div class="cta">
        <a class="btn primary" href="#contact">Get a Free Setup Plan</a>
        <a class="btn" href="YOUR_INSTAGRAM_LINK" target="_blank" rel="noreferrer">DM us on Instagram</a>
      </div>

      <div class="grid">
        <div class="card third">
          <h3>Instant response</h3>
          <p>New leads get a message in seconds — not hours.</p>
        </div>
        <div class="card third">
          <h3>Auto follow-up</h3>
          <p>We follow up until they reply or book.</p>
        </div>
        <div class="card third">
          <h3>Booked appointments</h3>
          <p>Leads get a link to schedule, plus reminders.</p>
        </div>
      </div>
    </section>

    <section id="how" class="section">
      <h2>How it works</h2>
      <p class="desc">A simple, clean setup that fits your business — no complicated tech.</p>

      <div class="steps">
        <div class="step">
          <div class="num">1</div>
          <strong>Connect your leads</strong>
          <p class="small">Website, Instagram, Google Business, ads, forms — wherever leads come in.</p>
        </div>
        <div class="step">
          <div class="num">2</div>
          <strong>Auto reply + follow up</strong>
          <p class="small">Instant message + reminders so people don’t fall through.</p>
        </div>
        <div class="step">
          <div class="num">3</div>
          <strong>Booking + reminders</strong>
          <p class="small">They book through your calendar, and we send confirmations/reminders.</p>
        </div>
      </div>

      <div class="grid">
        <div class="card half">
          <h3>Perfect for</h3>
          <p>Med spas • Dentists • Chiropractors • Barbershops • Contractors • Trainers • Any appointment-based business</p>
          <div>
            <span class="tag">After-hours leads</span>
            <span class="tag">Missed calls</span>
            <span class="tag">Slow replies</span>
            <span class="tag">No-shows</span>
          </div>
        </div>

        <div class="card half">
          <h3>What you’ll notice</h3>
          <p>
            Faster replies → more conversations → more booked calls.
            Most businesses lose money just from slow follow-ups.
          </p>
        </div>
      </div>
    </section>

    <section id="offer" class="section">
      <h2>What you get</h2>
      <p class="desc">We keep it simple and effective — built around getting you more booked appointments.</p>

      <div class="list">
        <div class="item"><div class="check"></div><div><strong>Lead capture</strong><div class="small">Forms + inquiry tracking so every lead is saved.</div></div></div>
        <div class="item"><div class="check"></div><div><strong>Instant response</strong><div class="small">Auto reply that feels natural and professional.</div></div></div>
        <div class="item"><div class="check"></div><div><strong>Follow-up sequences</strong><div class="small">Messages that keep going until they respond or book.</div></div></div>
        <div class="item"><div class="check"></div><div><strong>Booking calendar</strong><div class="small">Scheduling link + reminders to reduce no-shows.</div></div></div>
        <div class="item"><div class="check"></div><div><strong>Simple handoff</strong><div class="small">When they’re ready, you get notified to close fast.</div></div></div>
      </div>

      <div class="grid">
        <div class="card half">
          <h3>Timeline</h3>
          <p>Setup usually takes <strong>48–72 hours</strong> depending on what you need connected.</p>
        </div>
        <div class="card half">
          <h3>Price</h3>
          <p>We start with a free setup plan — then a simple one-time setup + monthly support (if you want ongoing).</p>
        </div>
      </div>
    </section>

    <section id="contact" class="section">
      <h2>Get a free setup plan</h2>
      <p class="desc">Fill this out and we’ll send a quick plan showing exactly what to automate for your business.</p>

      <!-- This form uses mailto (works without backend). You can upgrade later. -->
      <form action="mailto:YOUR EMAIL" method="post" enctype="text/plain">
        <div class="row">
          <input name="Name" placeholder="Your name" required />
          <input name="Business" placeholder="Business name" required />
        </div>
        <div class="row">
          <input name="Phone" placeholder="Phone number (optional)" />
          <input name="Email" placeholder="Email" required />
        </div>
        <input name="Lead Source" placeholder="Where do leads come from? (IG, Google, website, ads, etc.)" />
        <textarea name="Goal" placeholder="What do you want more of? (booked appointments, calls, quotes, etc.)"></textarea>
        <button class="btn primary" type="submit">Send</button>
        <div class="small">Prefer DM? Message us: <a href="YOUR_INSTAGRAM_LINK" target="_blank" rel="noreferrer"><u>Instagram</u></a></div>
      </form>

      <div class="footer">
        <div>© <span id="y"></span> ShortFormLabs • Atlanta</div>
        <div>Lead automation • Follow-ups • Booking</div>
      </div>
    </section>
  </div>

  <script>
    document.getElementById('y').textContent = new Date().getFullYear();
  </script>
</body>
</html>
